import 'package:flutter/material.dart';
import 'package:flutter_capstone/widget/login-component.dart';

class Login extends StatelessWidget {
  const Login({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: LoginComponent(),
    );
  }
}
